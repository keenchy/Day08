// Only change code below this line

function randomRangeNumber(minNumer, maxNumber) {

    Math.floor(Math.random() * (maxNumber - minNumer + 1)) + min;
}


randomRangeNumber(6, 8);

// Onli change code above this line

module.expors = randomRangeNumber;